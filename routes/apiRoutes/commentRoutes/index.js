const router = require('express').Router();

// router.get('/:postId', viewComments)

// router.post('/', createComment);

// router.route('/:commentId')
//     .patch(updateComment)
//     .delete(deleteComment);

module.exports = router;
